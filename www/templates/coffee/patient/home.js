// Generated by CoffeeScript 1.6.3
(function() {
  var Doctor, Home;

  window.tokdoc.navbar.show_user_login(false);

  Doctor = (function() {
    function Doctor(email) {
      this.email = ko.observable(email);
    }

    Doctor.prototype.view_profile_click = function() {
      console.log('clicked ' + this.email());
      return window.location = '/doctor/' + this.email();
    };

    return Doctor;

  })();

  Home = (function() {
    function Home() {
      var load_doctors,
        _this = this;
      this.doctor_contacts = ko.observableArray([]);
      load_doctors = function(data) {
        var doctor, doctor_email, _i, _len, _ref;
        _ref = data.doctor_contacts;
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          doctor_email = _ref[_i];
          doctor = new Doctor(doctor_email);
          _this.doctor_contacts.push(doctor);
        }
        return console.log(data);
      };
      $.get('/api/get_doctors/', load_doctors);
    }

    return Home;

  })();

  window.tokdoc.home = new Home();

  ko.applyBindings(window.tokdoc.home, document.getElementById("patient_home"));

}).call(this);

/*
//@ sourceMappingURL=home.map
*/
